using UnityEngine;
using System.Collections;

public class PaddleEmbiggen : MonoBehaviour {

	// Use this for initialization
	void Start () {
        transform.localScale = new Vector3(3f,3f,1f); //How to change scale
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
